<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Property para nome da aplicação -->
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="tech-challenge-api"/>
    
    <!-- Console Appender - JSON Format (SRE Best Practices) -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- Campos customizados seguindo padrões SRE -->
            <customFields>{"application":"${appName}","environment":"dev","service":"tech-challenge-api"}</customFields>
            
            <!-- Incluir stack trace completo para ERROR -->
            <includeStacktrace>true</includeStacktrace>
            
            <!-- Incluir contexto MDC (para tracing e correlation) -->
            <includeMdc>true</includeMdc>
            
            <!-- Incluir informações de contexto -->
            <includeContext>true</includeContext>
            
            <!-- Incluir caller data (classe/método/linha) -->
            <includeCallerData>true</includeCallerData>
            
            <!-- Configuração de timestamp em UTC (padrão SRE) -->
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampPattern>
            <timeZone>UTC</timeZone>
            
            <!-- Campos do log estruturado -->
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <version>version</version>
                <message>message</message>
                <logger>logger_name</logger>
                <thread>thread</thread>
                <level>severity</level>
                <levelValue>[ignore]</levelValue>
                <stackTrace>stack_trace</stackTrace>
            </fieldNames>
            
            <!-- Adicionar campos extras do SRE -->
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <maxDepthPerThrowable>50</maxDepthPerThrowable>
                <maxLength>8192</maxLength>
                <shortenedClassNameLength>30</shortenedClassNameLength>
                <exclude>sun\.reflect\..*</exclude>
                <exclude>net\.sf\.cglib\..*</exclude>
                <evaluator class="ch.qos.logback.classic.boolex.OnMarkerEvaluator">
                    <marker>SHORTENED</marker>
                </evaluator>
            </throwableConverter>
        </encoder>
    </appender>
    
    <!-- Configuração única para ambiente DEV (logs em JSON) -->
    <root level="INFO">
        <appender-ref ref="JSON_CONSOLE"/>
    </root>
    
    <!-- Loggers da aplicação - Nível DEBUG para desenvolvimento -->
    <logger name="com.fiap.techchallenge" level="INFO" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- Loggers específicos por categoria SRE -->
    
    <!-- WARN: Spring Framework (problemas de configuração não críticos) -->
    <logger name="org.springframework" level="WARN" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- INFO: Spring Boot (inicialização e configuração) -->
    <logger name="org.springframework.boot" level="INFO" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- INFO: Spring Security (autenticação e autorização) -->
    <logger name="org.springframework.security" level="INFO" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- WARN: Hibernate (performance issues, N+1 queries) -->
    <logger name="org.hibernate" level="WARN" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- WARN: SQL queries (apenas em caso de problemas) -->
    <logger name="org.hibernate.SQL" level="WARN" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- WARN: Connection pool (problemas de conexão) -->
    <logger name="com.zaxxer.hikari" level="WARN" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- WARN: Tomcat (problemas de servidor) -->
    <logger name="org.apache.catalina" level="WARN" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    <logger name="org.apache.tomcat" level="WARN" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
    
    <!-- ERROR: AWS SDK (falhas de integração críticas) -->
    <logger name="software.amazon.awssdk" level="ERROR" additivity="false">
        <appender-ref ref="JSON_CONSOLE"/>
    </logger>
</configuration>
